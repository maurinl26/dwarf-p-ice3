import cupy as cp


def test_cupy_take_interpolation():
    """
    Test 1D interpolation with cupy take and 
    a switch between numpy take and cupy take, whether the 
    backend is cpu or gpu.
    """
    ...