


def inf_error(ref, run) -> float:
    """Computes infinite error between a reference field
    and a field to compare.

    Args:
        ref (_type_): reference field
        run (_type_): field to compare

    Returns:
        float: infinite error
    """
    pass
   
    
    
def l2_norm_error(ref, run) -> float:
    """Computes mean squared error on a field between 
    a reference field and one for the run.

    Args:
        ref (_type_): reference field
        run (_type_): field to compare

    Returns:
        float: value of mse
    """
    pass